# Socially Engaging roBOtics Lab Official Website

## How to run a simple HTTP server to view the site locally

First, in a terminal, `cd` into this directory, then run:
```
python3 -m http.server
```
